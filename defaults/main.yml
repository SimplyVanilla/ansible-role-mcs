---

mcs_fork: paper

mcs_version: '1.20.4'

mcs_user: 'mcs'

mcs_path: '/home/mcs/server'

mcs_heap: 10

mcs_daily_restart: ''

mcs_autostart: true

mcs_restart_on_change: true

mcs_cpu_affinity: []

mcs_pufferfishplus_key: ''

mcs_flags: zgc

mcs_use_large_pages: false

mcs_restart_threshold: 18000

velocity_restart_threshold: 72000
