---

- hosts: localhost

  vars:
    mcs_fork: pufferfish
    mcs_version: '1.20'
    mcs_heap: 1
    mcs_flags: zgc

  tasks:
    - name: Import project role
      ansible.builtin.import_role:
        name: project
